# toggle_not_covered (cover)

Info severity - a single signal bit that never toggled, listed
individually alongside the `toggle_coverage_below_threshold` rollup that
actually fails the gate. See that file for the fix; this one just
pinpoints which bit.
