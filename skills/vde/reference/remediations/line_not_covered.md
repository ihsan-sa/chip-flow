# line_not_covered (cover)

Info severity - a single uncovered line, listed individually alongside
the `line_coverage_below_threshold` rollup that actually fails the gate.
See that file for the fix; this one just pinpoints where.
